# T-learn App

T-learn App — backend-приложение для изучения слов с помощью карточек, разработанное в рамках **программы «Проектный практикум» Т-Банка**.

Проект ориентирован на production-подходы: безопасность, тестируемость, наблюдаемость, CI/CD и нагрузочное тестирование.

---

## 🚀 Основные возможности

- Управление словарями и карточками
- JWT-аутентификация и авторизация
- Rate limiting на уровне пользователя
- Кэширование с использованием Redis
- Централизованная обработка ошибок
- Метрики и мониторинг (Prometheus + Grafana)

---

## 🏗 Архитектура

Проект построен по классической layered-архитектуре:

- **Controller** — REST API
- **Service** — бизнес-логика
- **Repository** — доступ к данным (Spring Data JPA)
- **DTO + Mapper** — разделение API и доменной модели
- **Security** — JWT, фильтры, Spring Security
- **Exception handling** — единый формат ошибок
- **Lombok** — сокращение кода и повышение читаемости

---

## 🔐 Безопасность

- JWT-аутентификация
- Кастомный `UserPrincipal`
- Spring Security Filter Chain
- Rate limiting для аутентифицированных пользователей  
  (на основе Resilience4j)

При превышении лимита запросов возвращается ошибка `429 Too Many Requests`
с поддержкой `Retry-After`.

---

## 🧪 Тестирование

- Unit-тесты сервисного слоя
- Проверки в CI:
  - Spotless (форматирование)
  - Checkstyle (код-стайл)
  - SpotBugs (статический анализ)
  - Maven tests (`verify`)
- Нагрузочное тестирование с использованием k6

Всего в проекте 12 тестов.

---

## 📊 Мониторинг и Observability

- **Prometheus** — сбор метрик
- **Grafana** — визуализация
- HTTP-метрики и метрики приложения
- Возможность расширения кастомными метриками

---

## 🐳 Docker и окружения

Поддерживаются разные окружения:

- `docker-compose.dev.yml` — для локальной разработки
- `docker-compose.prod.yml` — для production

Включены сервисы:
- Backend (Spring Boot)
- База данных
- Redis
- Prometheus
- Grafana

---

## 🔁 CI/CD

### Pull Request Checks (GitHub Actions)
При каждом PR автоматически выполняются:
- Spotless
- Checkstyle
- SpotBugs
- Тесты

### Deploy pipeline
- Ручной деплой по кнопке
- Деплой на сервер по SSH
- Сборка и запуск через Docker Compose

---

## 🛠 Технологии

- Java 21
- Spring Boot
- Spring Security
- Spring Data JPA
- JWT
- Redis
- Resilience4j
- Lombok
- Docker / Docker Compose
- GitHub Actions
- Prometheus + Grafana
- k6

---

## 🎓 Контекст проекта

Проект разработан в рамках **программы проектного практикума Т-Банка**  
и демонстрирует практики, применяемые в реальной production-разработке backend-сервисов.

---

## 📌 Статус проекта

Проект активно развивается и может быть расширен
новыми функциями, тестами и метриками.
